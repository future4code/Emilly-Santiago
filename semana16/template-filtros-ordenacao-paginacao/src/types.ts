export type product = {
  id: string;
  name: string;
  image_url: string;
  user_Id: string;
  price: number;
};

export type user = {
   id: string
   name: string
   email: string
   password: string
}